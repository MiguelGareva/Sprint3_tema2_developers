<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Actualizar Tarea</title>
</head>
<body>
    <h1>Actualizar Tarea</h1>
    <?php
   
    $task = $this->task; ?>

    <form method="post">
        <input type="hidden" name="id" value="<?php echo $task['id']; ?>"> 

        <div>
            <label for="description">Descripción:</label>
            <input type="text" id="description" name="description" value="<?php echo $task['description']; ?>" required>
        </div>
        
        <div>
            <label for="status">Estado:</label>
            <select id="status" name="status" required>
                <option value="initialized" <?php if ($task['status'] == 'initialized') echo 'selected'; ?>>Inicializado</option>
                <option value="in process" <?php if ($task['status'] == 'in process') echo 'selected'; ?>>En proceso</option>
                <option value="finalized" <?php if ($task['status'] == 'finalized') echo 'selected'; ?>>Finalizado</option>
            </select>
        </div>
        
        <div>
            <label for="user">Usuario:</label>
            <input type="text" id="user" name="user" value="<?php echo $task['user']; ?>" required>
        </div>
        
        <div>
            <label for="date_ini">Fecha de Inicio:</label>
            <input type="datetime-local" id="date_ini" name="date_ini" value="<?php echo date('Y-m-d\TH:i', strtotime($task['date_ini'])); ?>" required>
        </div>
        
        <div>
            <label for="date_end">Fecha de Fin:</label>
            <input type="datetime-local" id="date_end" name="date_end" value="<?php echo date('Y-m-d\TH:i', strtotime($task['date_end'])); ?>" required>
        </div>
        
        <button type="submit">Guardar Tarea</button>
    </form>

    <a href="./">Volver a la Lista</a>
</body>
</html>